extern void Build_tree();
int Find_ngb_tree(const size_t, const float, int *);
extern int *Find_ngb_tree_recursive(size_t, float, int);
extern float Guess_hsml(const size_t ipart, const int DesNumNgb);
